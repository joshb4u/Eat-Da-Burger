<!-- Jumbotron -->
<header>
    <div class="jumbotron jumbotron-fluid">
        <div class="container">
            <div class="row">
                <div class="col">
                    <h1 class="display-4 text-center">Eat-Da-Burger!</h1>
                    <p class="lead"></p>
                </div>
            </div>
        </div>
    </div>

</header>
<!-- End Jumbotron -->

<main class="container">
    <aside class="container">
        <div class="row text-center bg-light">
            <div class="card card-body">
                <form id="form" class="form-inline align-items-center create-form">
                    <div class="col-md-8">
                        <div class="input-group md-2">
                            <input type="text" id="inputBurger" class="form-control"
                                placeholder="Enter a burger you want to eat! Click the Submit button." />
                        </div>
                    </div>
                    <div class="col">
                        <button type="submit" id="submitBurger" class="btn btn-primary submitBurger">
                            Submit
                        </button>
                    </div>
                </form>
            </div>
        </div>
        </div>
    </aside>
    <aside class="container">
        <div class="row mt-5">
            <div class="col-md-6">
                <div class="list-group">
                    {{#each burgers}}
                    {{#unless devoured}}
                    <a href="#" class="list-group-item list-group-item-action">
                        {{id}}. {{burgerName}}
                        <button data-id="{{id}}" class="btn btn-primary ml-3 eatBurger">
                            Devour it!
                        </button>
                    </a>
                    {{/unless}}
                    {{/each}}
                </div>
            </div>
            <div class="col-md-6">
                <div class="list-group">
                    {{#each burgers}}
                    {{#if devoured}}
                    <a href="#" class="list-group-item list-group-item-action disabled">
                        {{id}}. {{burgerName}}
                    </a>
                    {{/if}}
                    {{/each}}
                </div>
            </div>
        </div>
    </aside>
</main>